# QueryCraft v1.1.0 - คู่มือการใช้งานฉบับสมบูรณ์

<img src="src/main/resources/images/logo.png" width="100" height="100" align="center">

## สารบัญ
1. [บทนำ](#1-บทนำ)
2. [ความต้องการของระบบ](#2-ความต้องการของระบบ)
3. [การติดตั้งและตั้งค่า](#3-การติดตั้งและตั้งค่า)
4. [ภาพรวมส่วนติดต่อผู้ใช้](#4-ภาพรวมส่วนติดต่อผู้ใช้)
5. [การเชื่อมต่อฐานข้อมูล](#5-การเชื่อมต่อฐานข้อมูล)
6. [การเขียนและรันคำสั่ง SQL](#6-การเขียนและรันคำสั่ง-sql)
7. [การทำงานกับผลลัพธ์](#7-การทำงานกับผลลัพธ์)
8. [การส่งออกข้อมูล](#8-การส่งออกข้อมูล)
9. [โหมดประมวลผลแบบแบทช์](#9-โหมดประมวลผลแบบแบทช์)
10. [การตั้งค่าและการกำหนดค่า](#10-การตั้งค่าและการกำหนดค่า)
11. [ฟีเจอร์ความปลอดภัย](#11-ฟีเจอร์ความปลอดภัย)
12. [การแก้ไขปัญหา](#12-การแก้ไขปัญหา)
13. [คีย์ลัด](#13-คีย์ลัด)

---

## 1. บทนำ

QueryCraft เป็นแอปพลิเคชันเดสก์ท็อปที่พัฒนาด้วย JavaFX สำหรับการ query ฐานข้อมูล การสำรวจข้อมูล และการส่งออกข้อมูล มีประสิทธิภาพสูง ให้คุณเชื่อมต่อและทำงานกับหลายระบบฐานข้อมูลได้ในหน้าจอเดียว รองรับการจัดการชุดข้อมูลขนาดใหญ่ผ่านฟีเจอร์ขั้นสูง เช่น การจำกัดจำนวนแถวแบบไดนามิก และโหมดสตรีมมิ่ง

### ความสามารถหลัก
- **รองรับหลายฐานข้อมูล**: MySQL, PostgreSQL, Microsoft SQL Server และไฟล์ CSV
- **การรัน Query ขั้นสูง**: โหมดมาตรฐานและโหมดสตรีมมิ่งสำหรับชุดข้อมูลตั้งแต่หลักร้อยถึงหลักล้านแถว
- **การส่งออกข้อมูล**: ส่งออกเป็น CSV ด้วยการเข้ารหัสที่กำหนดเอง และสร้างคำสั่ง SQL INSERT
- **การประมวลผลแบทช์**: ระบบอัตโนมัติ "เลือก → ส่งออก → ลบ" สำหรับการเก็บถาวรข้อมูล
- **ความปลอดภัย**: ตรวจสอบ SQL ในตัว กล่องโต้ตอบยืนยันสำหรับการดำเนินการที่เป็นอันตราย

---

## 2. ความต้องการของระบบ

### ความต้องการขั้นต่ำ
- **ระบบปฏิบัติการ**: Windows 10/11, macOS 10.14+ หรือ Linux (Ubuntu 18.04+)
- **Java Runtime**: JDK 17 หรือสูงกว่า
- **หน่วยความจำ**: RAM 4 GB (แนะนำ 8 GB สำหรับชุดข้อมูลขนาดใหญ่)
- **พื้นที่ดิสก์**: 200 MB สำหรับแอปพลิเคชัน พื้นที่เพิ่มเติมสำหรับไฟล์ส่งออก
- **จอภาพ**: ความละเอียด 1280x720 หรือสูงกว่า

### ความต้องการที่แนะนำ
- **หน่วยความจำ**: RAM 8 GB หรือมากกว่า
- **จอภาพ**: ความละเอียด 1920x1080
- **อินเทอร์เน็ต**: จำเป็นสำหรับดาวน์โหลดไดรเวอร์ JDBC (รันแรก)

---

## 3. การติดตั้งและตั้งค่า

### 3.1 การติดตั้งบน Windows

1. **ดาวน์โหลด** แพคเกจ QueryCraft
2. **แตกไฟล์** ไปยังตำแหน่งที่ต้องการ (เช่น `C:\Program Files\QueryCraft`)
3. **รัน** ไฟล์ `run.bat` เพื่อเริ่มแอปพลิเคชัน
4. **ทางเลือก**: สร้างทางลัดบนเดสก์ท็อปไปยัง `run.bat`

### 3.2 การสร้างจากซอร์สโค้ด

```bash
# โคลน repository
git clone https://github.com/sutthiphatchuenban/querycraft_beta.git
cd querycraft

# สร้างโปรเจกต์
mvn clean package

# รันแอปพลิเคชัน
mvn javafx:run
```

### 3.3 การเปิดใช้งานครั้งแรก

เมื่อคุณเริ่ม QueryCraft ครั้งแรก:
1. หน้าจอ Splash Screen จะปรากฏขณะที่แอปพลิเคชันกำลังเริ่มต้น
2. ไดรเวอร์ฐานข้อมูลจะถูกโหลดโดยอัตโนมัติ
3. หน้าต่างหลักจะเปิดขึ้นพร้อมการตั้งค่าเริ่มต้น

---

## 4. ภาพรวมส่วนติดต่อผู้ใช้

อินเทอร์เฟซ QueryCraft แบ่งออกเป็นหลายพื้นที่หลัก:

```
┌──────────────────────────────────────────────────────────────────┐
│  [เชื่อมต่อ...] [ตัดการเชื่อมต่อ] [ช่วยเหลือ] [ตั้งค่า]   สถานะ: พร้อม │
├──────────┬───────────────────────────────────────────────────────┤
│          │  ตัวแก้ไขคำสั่ง SQL:                                    │
│  ตาราง   │  ┌─────────────────────────────────────────────────┐ │
│  ──────  │  │ SELECT * FROM users WHERE status = 'active'     │ │
│  users   │  │                                                 │ │
│  orders  │  └─────────────────────────────────────────────────┘ │
│  ...     │  [รัน Query] [ล้างทั้งหมด] [จัดรูปแบบ SQL]            │
│          │                                                        │
│  Query   │  ผลลัพธ์ (150 แถว):                                     │
│  ล่าสุด  │  ┌─────────────────────────────────────────────────┐ │
│  ──────  │  │ id │ ชื่อ  │ อีเมล          │ สถานะ │ สร้างเมื่อ │ │
│  Query 1 │  ├─────────────────────────────────────────────────┤ │
│  Query 2 │  │ 1  │ John  │ john@mail.com  │ active │ 2024-01  │ │
│  ...     │  └─────────────────────────────────────────────────┘ │
│          │  [ส่งออก CSV] [สร้าง SQL]                              │
└──────────┴───────────────────────────────────────────────────────┘
```

### 4.1 แถบด้านบน
- **เชื่อมต่อ...**: เปิดกล่องโต้ตอบการเชื่อมต่อ
- **ตัดการเชื่อมต่อ**: ปิดการเชื่อมต่อฐานข้อมูลปัจจุบัน
- **ช่วยเหลือ**: เปิดเอกสารประกอบในตัว
- **ตั้งค่า**: กำหนดค่าการตั้งค่าแอปพลิเคชัน
- **แสดงสถานะ**: แสดงข้อมูลการเชื่อมต่อและฐานข้อมูลปัจจุบัน

### 4.2 แถบด้านซ้าย
- **รายการตาราง**: แสดงตารางทั้งหมดในฐานข้อมูลที่เชื่อมต่อ
- **Query ล่าสุด**: ประวัติคำสั่ง SQL ที่รันล่าสุด (50 รายการ)

### 4.3 พื้นที่ตรงกลาง
- **ตัวแก้ไข SQL**: เขียนและแก้ไขคำสั่ง SQL พร้อมไฮไลต์ไวยากรณ์
- **ตารางผลลัพธ์**: แสดงผลลัพธ์ query พร้อมการแบ่งหน้าและการกรอง

### 4.4 แถบด้านล่าง
- **ป้ายสถานะ**: แสดงสถานะการรันและข้อความ
- **ข้อมูลเวอร์ชัน**: เวอร์ชันแอปพลิเคชันปัจจุบัน

---

## 5. การเชื่อมต่อฐานข้อมูล

### 5.1 ประเภทฐานข้อมูลที่รองรับ

| ฐานข้อมูล | พอร์ตเริ่มต้น | ไดรเวอร์ | ฟีเจอร์พิเศษ |
|----------|-------------|--------|------------------|
| MySQL | 3306 | com.mysql.cj.jdbc.Driver | LIMIT clause, backtick escaping |
| PostgreSQL | 5432 | org.postgresql.Driver | CTE support, ILIKE operator |
| SQL Server | 1433 | com.microsoft.sqlserver.jdbc.SQLServerDriver | TOP clause, Windows Auth |
| CSV (H2) | N/A | org.h2.Driver | File-based, auto-detect encoding |

### 5.2 การสร้างการเชื่อมต่อ

1. คลิก **เชื่อมต่อ...** ในแถบด้านบน
2. เลือกประเภทฐานข้อมูลจากรายการแบบเลื่อนลง
3. กรอกพารามิเตอร์การเชื่อมต่อ:
   - **โฮสต์**: ที่อยู่เซิร์ฟเวอร์ (เช่น `localhost`, `192.168.1.100`)
   - **พอร์ต**: พอร์ตเซิร์ฟเวอร์ (กรอกอัตโนมัติด้วยค่าเริ่มต้น)
   - **ฐานข้อมูล**: ชื่อฐานข้อมูล
   - **ชื่อผู้ใช้**: ชื่อผู้ใช้ฐานข้อมูลของคุณ
   - **รหัสผ่าน**: รหัสผ่านฐานข้อมูลของคุณ
4. ทางเลือก: ติ๊ก "ใช้ SSL" สำหรับการเชื่อมต่อที่เข้ารหัส
5. ทางเลือก: ติ๊ก "จดจำการเชื่อมต่อ" เพื่อบันทึกการตั้งค่า
6. คลิก **ทดสอบการเชื่อมต่อ** เพื่อตรวจสอบ
7. คลิก **เชื่อมต่อ** เพื่อสร้างการเชื่อมต่อ

### 5.3 การเชื่อมต่อ MySQL

**การเชื่อมต่อมาตรฐาน:**
- โฮสต์: `localhost` หรือ IP เซิร์ฟเวอร์ของคุณ
- พอร์ต: `3306` (ค่าเริ่มต้น)
- ฐานข้อมูล: ชื่อฐานข้อมูลของคุณ
- ชื่อผู้ใช้: ชื่อผู้ใช้ MySQL ของคุณ
- รหัสผ่าน: รหัสผ่าน MySQL ของคุณ

**Cloud MySQL (Neon.tech, PlanetScale):**
- เปิดใช้งานช่องทำเครื่องหมาย **ใช้ SSL**
- ใช้ชื่อโฮสต์ที่ผู้ให้บริการ cloud จัดหาให้
- พอร์ตโดยทั่วไปคือ `3306` หรือตามที่ผู้ให้บริการระบุ

**ตัวอย่าง Connection String:**
```
jdbc:mysql://localhost:3306/mydb?useSSL=false&serverTimezone=UTC
```

### 5.4 การเชื่อมต่อ PostgreSQL

**การเชื่อมต่อมาตรฐาน:**
- โฮสต์: `localhost` หรือที่อยู่เซิร์ฟเวอร์ของคุณ
- พอร์ต: `5432` (ค่าเริ่มต้น)
- ฐานข้อมูล: ชื่อฐานข้อมูล
- ชื่อผู้ใช้: ชื่อผู้ใช้ PostgreSQL ของคุณ
- รหัสผ่าน: รหัสผ่าน PostgreSQL ของคุณ

**หมายเหตุการเชื่อมต่อ:**
- PostgreSQL มีความเข้มงวดกว่าเกี่ยวกับตัวระบุที่อยู่ในเครื่องหมายคำพูด (case-sensitive)
- ใช้ `ILIKE` สำหรับการจับคู่ข้อความแบบไม่คำนึงถึงตัวพิมพ์เล็ก/ใหญ่
- รองรับ CTE (Common Table Expressions) ด้วยคำสั่ง `WITH`

### 5.5 การเชื่อมต่อ SQL Server

**การเชื่อมต่อมาตรฐาน:**
- โฮสต์: `localhost` หรือที่อยู่เซิร์ฟเวอร์ของคุณ
- พอร์ต: `1433` (ค่าเริ่มต้น)
- ฐานข้อมูล: ชื่อฐานข้อมูล
- ชื่อผู้ใช้: ชื่อผู้ใช้รับรองความถูกต้องของ SQL Server
- รหัสผ่าน: รหัสผ่านรับรองความถูกต้องของ SQL Server

**รับรองความถูกต้องของ Windows:**
- ติ๊ก "ใช้รับรองความถูกต้องของ Windows"
- ช่องชื่อผู้ใช้และรหัสผ่านจะถูกปิดใช้งาน
- ต้องใช้ `mssql-jdbc_auth-12.6.1.x64.dll` ในไดเรกทอรีแอปพลิเคชัน

**Named Pipes:**
- ติ๊ก "ใช้ Named Pipes"
- ใส่ชื่ออินสแตนซ์ (เช่น `MSSQLSERVER`, `SQLEXPRESS`)
- พอร์ตจะไม่ถูกใช้ในโหมด Named Pipes

### 5.6 การเชื่อมต่อไฟล์ CSV

1. เลือก **CSV File (H2)** เป็นประเภทฐานข้อมูล
2. คลิก **Browse...** เพื่อเลือกโฟลเดอร์ที่มีไฟล์ CSV
3. ไฟล์ `.csv` ทั้งหมดในโฟลเดอร์จะถูกโหลดเป็นตาราง
4. คลิก **เชื่อมต่อ**

**กฎสำคัญสำหรับ CSV:**
- **ต้องใช้เครื่องหมายคำพูดคู่** รอบชื่อตารางและคอลัมน์เสมอ
- ชื่อไฟล์กลายเป็นชื่อตาราง (เช่น `customers.csv` → `"customers"`)
- แถวแรกถือเป็นหัวตาราง (ชื่อคอลัมน์)
- H2 ตรวจจับการเข้ารหัส UTF-8, TIS-620, Windows-874 โดยอัตโนมัติ
- ตัวคั่นที่รองรับ: จุลภาค, อัฒภาค, แท็บ

**ตัวอย่าง Query CSV:**
```sql
SELECT * FROM "customers" WHERE "country" = 'Thailand';

SELECT c."name", o."order_date"
FROM "customers" c
JOIN "orders" o ON c."id" = o."customer_id";
```

### 5.7 การจัดการการเชื่อมต่อล่าสุด

- บันทึกการเชื่อมต่อล่าสุดได้สูงสุด **20 รายการ**
- เลือกจากรายการแบบเลื่อนลง "ล่าสุด" เพื่อกรอกรายละเอียดการเชื่อมต่อโดยอัตโนมัติ
- คลิก **ลบ** เพื่อลบการเชื่อมต่อที่บันทึกไว้
- รหัสผ่านถูกทำให้ไม่สามารถอ่านได้ (Base64) เพื่อความปลอดภัยขั้นพื้นฐาน

---

## 6. การเขียนและรันคำสั่ง SQL

### 6.1 ฟีเจอร์ตัวแก้ไข SQL

ตัวแก้ไข SQL ให้:
- **ไฮไลต์ไวยากรณ์** สำหรับคำสำคัญ SQL
- **การเติมคำอัตโนมัติ** สำหรับชื่อตารางและคำสำคัญ SQL
- **หมายเลขบรรทัด** เพื่อการนำทางที่ง่ายขึ้น
- **รองรับหลายคำสั่ง** (คั่นด้วยเครื่องหมายอัฒภาค)

### 6.2 การรันคำสั่ง

**การรันมาตรฐาน:**
1. พิมพ์คำสั่ง SQL ของคุณในตัวแก้ไข
2. กด **Ctrl+Enter** (หรือ Cmd+Enter บน Mac)
3. ผลลัพธ์จะปรากฏในตารางด้านล่าง

**พร้อมพารามิเตอร์:**
1. เขียน SQL ด้วยพารามิเตอร์ชื่อโดยใช้ไวยากรณ์ `:paramName`
2. กด **Ctrl+Enter**
3. กล่องโต้ตอบพารามิเตอร์จะปรากฏขึ้นเพื่อขอค่า
4. ใส่ค่าแล้วคลิก OK เพื่อรัน

**ตัวอย่าง Query ที่มีพารามิเตอร์:**
```sql
SELECT * FROM customers 
WHERE country = :country 
  AND created_at >= :startDate;
```

### 6.3 ประเภทคำสั่ง

**คำสั่ง SELECT:**
- แสดงผลลัพธ์ในตารางแบบแบ่งหน้า
- แสดงจำนวนแถวในแถบสถานะ
- สามารถกรอง ส่งออก หรือคัดลอกผลลัพธ์ได้

**คำสั่ง INSERT/UPDATE/DELETE:**
- แสดงจำนวนแถวที่ได้รับผลกระทบ
- กล่องโต้ตอบยืนยันปรากฏสำหรับการดำเนินการ DELETE
- ไม่แสดงตารางผลลัพธ์

### 6.4 โหมดสตรีมมิ่ง

สำหรับชุดข้อมูลขนาดใหญ่ (100,000+ แถว):

1. คลิกปุ่ม **Streaming: OFF** เพื่อสลับเป็น **Streaming: ON**
2. รันคำสั่ง SELECT ของคุณ
3. ผลลัพธ์จะสตรีมเข้ามาทีละส่วน
4. การใช้หน่วยความจำจะคงที่ไม่ว่าขนาดผลลัพธ์จะเป็นเท่าใด

**เมื่อใดควรใช้ Streaming:**
- Query ตารางที่มี >50,000 แถว
- ส่งออกชุดข้อมูลขนาดใหญ่
- ป้องกันข้อผิดพลาด OutOfMemory

### 6.5 การจัดรูปแบบ SQL

กด **Ctrl+F** เพื่อจัดรูปแบบ SQL โดยอัตโนมัติ:
```sql
-- ก่อนจัดรูปแบบ:
select id,name,email from users where status='active' and created_at>'2024-01-01';

-- หลังจัดรูปแบบ:
SELECT id, name, email
FROM users
WHERE status = 'active'
  AND created_at > '2024-01-01';
```

### 6.6 การทำงานกับแถบด้านข้าง

**รายการตาราง:**
- **ดับเบิ้ลคลิก** ตาราง: สร้าง `SELECT * FROM table LIMIT 100`
- **คลิกขวา** → "Describe Structure": แสดงข้อมูลคอลัมน์
- **คลิกขวา** → "SELECT * (Top 100)": เหมือนกับดับเบิ้ลคลิก

**Query ล่าสุด:**
- **ดับเบิ้ลคลิก** query: โหลดลงในตัวแก้ไข
- เก็บ 50 คำสั่งล่าสุดที่รัน

---

## 7. การทำงานกับผลลัพธ์

### 7.1 ฟีเจอร์ตารางผลลัพธ์

- **การแบ่งหน้า**: นำทางผ่านชุดผลลัพธ์ขนาดใหญ่ (100 แถวต่อหน้า)
- **การจัดเรียงคอลัมน์**: คลิกหัวคอลัมน์เพื่อจัดเรียง
- **การปรับขนาดคอลัมน์**: ลากขอบคอลัมน์เพื่อปรับขนาด
- **การเลือก**: คลิกเซลล์เพื่อเลือก ใช้ Ctrl+Click สำหรับหลายรายการ
- **การคัดลอก**: เลือกเซลล์แล้วกด Ctrl+C เพื่อคัดลอก

### 7.2 การกรองผลลัพธ์

1. พิมพ์ในช่อง **กรองผลลัพธ์** ด้านบนตาราง
2. การกรองเกิดขึ้นโดยอัตโนมัติหลังจากหน่วงเวลาสั้นๆ
3. จะแสดงเฉพาะแถวที่มีข้อความตัวกรอง
4. การกรองไม่คำนึงถึงตัวพิมพ์เล็ก/ใหญ่และค้นหาทุกคอลัมน์

### 7.3 ตัวควบคุมการแบ่งหน้า

- **ก่อนหน้า**: ไปยังหน้าก่อนหน้า
- **ถัดไป**: ไปยังหน้าถัดไป
- ข้อมูลหน้าแสดง: `แสดง X-Y จาก Z รายการ (หน้า N จาก M)`

### 7.4 คำเตือนการตัดแถว

หากผลลัพธ์คำสั่งของคุณเกินขีดจำกัดจำนวนแถวสูงสุด:
- แบนเนอร์คำเตือนสีเหลืองจะปรากฏ: `[!] RESULTS TRUNCATED`
- เปิดใช้งานโหมดสตรีมมิ่งเพื่อดูแถวทั้งหมด
- หรือเพิ่มขีดจำกัดจำนวนแถวสูงสุดในการตั้งค่า

---

## 8. การส่งออกข้อมูล

### 8.1 ส่งออกเป็น CSV

1. รันคำสั่ง SELECT
2. คลิกปุ่ม **ส่งออกเป็น CSV...**
3. กำหนดค่าตัวเลือกการส่งออก:
   - **รูปแบบ**: Standard CSV หรือ Excel Compatible
   - **การเข้ารหัส**: UTF-8, UTF-8 with BOM, TIS-620, Windows-874, ฯลฯ
   - **ตัวคั่น**: จุลภาค, อัฒภาค, แท็บ, ท่อ, หรือกำหนดเอง
   - **รวมหัวตาราง**: รวมชื่อคอลัมน์ในแถวแรก
   - **ใส่เครื่องหมายคำพูดทุกค่า**: ห่อค่าทั้งหมดด้วยเครื่องหมายคำพูด
   - **รูปแบบวันที่**: ปรับแต่งรูปแบบวัน/เวลา
4. เลือกไฟล์ปลายทาง
5. คลิก **ส่งออก**

### 8.2 สร้างคำสั่ง SQL INSERT

1. รันคำสั่ง SELECT
2. คลิกปุ่ม **สร้าง SQL INSERTs...**
3. ใส่ชื่อตารางเป้าหมาย (เช่น `archived_customers`)
4. เลือกไฟล์ปลายทาง
5. คลิก **บันทึก**

ไฟล์ SQL ที่สร้างขึ้นประกอบด้วย:
- คอมเมนต์ส่วนหัวด้วย metadata
- ตัวห่อธุรกรรม (BEGIN/COMMIT)
- คำสั่ง INSERT แยกสำหรับแต่ละแถว
- การปรับแต่งไวยากรณ์เฉพาะฐานข้อมูล

### 8.3 ตารางอ้างอิงตัวเลือกการส่งออก

| ตัวเลือก | ค่า | คำอธิบาย |
|--------|--------|-------------|
| รูปแบบ | Standard, Excel | รูปแบบ Excel เพิ่ม BOM เพื่อความเข้ากันได้กับ Excel |
| การเข้ารหัส | UTF-8, UTF-8 BOM, TIS-620, Windows-874, UTF-16LE, ASCII, ISO-8859-1 | การเข้ารหัสอักขระสำหรับไฟล์ผลลัพธ์ |
| ตัวคั่น | จุลภาค, อัฒภาค, แท็บ, ท่อ, อื่นๆ | อักขระแยกฟิลด์ |
| รวมหัวตาราง | เปิด/ปิด | รวมชื่อคอลัมน์เป็นแถวแรก |
| ใส่เครื่องหมายคำพูดทุกค่า | เปิด/ปิด | ห่อค่าทั้งหมดด้วยเครื่องหมายคำพูด |
| รูปแบบวันที่ | Pattern | รูปแบบ Java SimpleDateFormat |

---

## 9. โหมดประมวลผลแบบแบทช์

การประมวลผลแบบแบทช์ทำงานอัตโนมัติของเวิร์กโฟล "เลือก → ส่งออก → ลบ" สำหรับการเก็บถาวรข้อมูล

### 9.1 เปิดใช้งานโหมดแบทช์

1. คลิกสลับ **Batch Action Mode** ในตัวแก้ไข query
2. ตัวแก้ไขสองตัวจะปรากฏ:
   - **บน**: คำสั่ง SELECT เพื่อระบุระเบียนสำหรับเก็บถาวร
   - **ล่าง**: คำสั่ง DELETE เพื่อลบระเบียนที่เก็บถาวร

### 9.2 เวิร์กโฟลการประมวลผลแบทช์

1. **เขียนคำสั่ง SELECT**: กำหนดระเบียนที่จะเก็บถาวร
   ```sql
   SELECT * FROM logs WHERE created_at < '2023-01-01';
   ```

2. **เขียนคำสั่ง DELETE**: กำหนดระเบียนที่จะลบ
   ```sql
   DELETE FROM logs WHERE created_at < '2023-01-01';
   ```

3. **คลิกปุ่ม Process Batch**
4. ระบบประมาณจำนวนแถว
5. กำหนดค่าการส่งออก (ตำแหน่งไฟล์ CSV)
6. สำหรับฐานข้อมูล: ทางเลือกสร้างไฟล์ SQL INSERT
7. ระบบส่งออกข้อมูลเป็น CSV
8. ระบบรันคำสั่ง DELETE
9. กล่องโต้ตอบสรุปแสดงผลลัพธ์

### 9.3 ความปลอดภัยการประมวลผลแบทช์

- ประมาณจำนวนแถวก่อนรัน
- การส่งออกต้องสำเร็จก่อนที่ DELETE จะรัน
- กล่องโต้ตอบยืนยันก่อนเริ่ม
- การจัดการความสำเร็จบางส่วนหาก DELETE ล้มเหลวหลังจากส่งออก

---

## 10. การตั้งค่าและการกำหนดค่า

### 10.1 แท็บการตั้งค่า Query

**Query Timeout (วินาที):**
- ช่วง: 5-300 วินาที
- ค่าเริ่มต้น: 30 วินาที
- ระยะเวลารอการรัน query ก่อนยกเลิก

**ขีดจำกัดจำนวนแถวสูงสุด:**
- ช่วง: 100-50,000 แถว
- ค่าเริ่มต้น: 10,000 แถว
- จำนวนแถวสูงสุดที่ดึงต่อ query (โหมดปกติ)

**จัดรูปแบบ SQL อัตโนมัติเมื่อวาง:**
- จัดรูปแบบ SQL โดยอัตโนมัติเมื่อวางลงในตัวแก้ไข

**ยืนยันการดำเนินการ DELETE:**
- แสดงกล่องโต้ตอบยืนยันก่อนรันคำสั่ง DELETE

### 10.2 แท็บการตั้งค่าการเชื่อมต่อ

**ขนาด Pool สูงสุด:**
- ช่วง: 2-50 การเชื่อมต่อ
- ค่าเริ่มต้น: 10 การเชื่อมต่อ
- จำนวนการเชื่อมต่อสูงสุดใน connection pool

**Connection Timeout (วินาที):**
- ช่วง: 5-120 วินาที
- ค่าเริ่มต้น: 30 วินาที
- หมดเวลาสำหรับการสร้างการเชื่อมต่อใหม่

**เปิดใช้งาน SSL โดยค่าเริ่มต้น:**
- เปิดใช้งาน SSL สำหรับการเชื่อมต่อใหม่โดยค่าเริ่มต้น

### 10.3 การบันทึกการตั้งค่า

การตั้งค่าจะถูกบันทึกโดยอัตโนมัติไปยัง:
- **Windows**: Windows Registry (`HKEY_CURRENT_USER\Software\JavaSoft\Prefs\querycraft`)
- **macOS**: `~/Library/Preferences/com.querycraft.plist`
- **Linux**: `~/.java/.userPrefs/querycraft/`

---

## 11. ฟีเจอร์ความปลอดภัย

### 11.1 การตรวจสอบ SQL

QueryCraft ตรวจสอบคำสั่ง SQL ทั้งหมดก่อนรันและบล็อก:
- `DROP TABLE`, `DROP DATABASE`
- `TRUNCATE TABLE`
- `ALTER DATABASE`, `ALTER SYSTEM`
- `GRANT ALL`, `REVOKE ALL`
- `SHUTDOWN`, `KILL`

### 11.2 กล่องโต้ตอบยืนยัน

**การดำเนินการ DELETE:**
- แสดงกล่องโต้ตอบยืนยันเสมอ
- คำเตือน: "การดำเนินการนี้เป็นคำสั่ง DELETE และจะแก้ไขข้อมูลถาวร"

**การดำเนินการ UPDATE/INSERT:**
- แสดงการยืนยันสำหรับการดำเนินการที่ไม่ใช่ SELECT
- คำเตือน: "การดำเนินการนี้จะแก้ไขข้อมูลในฐานข้อมูล"

### 11.3 Query Timeout

- Query หมดเวลาโดยอัตโนมัติหลังจากระยะเวลาที่กำหนด
- ป้องกัน query ที่ไม่มีที่สิ้นสุดจากการแฮงค์แอปพลิเคชัน
- โหมดสตรีมมิ่งมีหมดเวลาแยก (60 วินาที)

### 11.4 การจำกัดจำนวนแถว

- ขีดจำกัดเริ่มต้น 10,000 แถวป้องกัน query ขนาดใหญ่โดยไม่ได้ตั้งใจ
- แสดงคำเตือนการตัดแถวอย่างชัดเจน
- โหมดสตรีมมิ่งพร้อมใช้งานสำหรับ query ขนาดใหญ่ที่ถูกต้องตามกฎหมาย

### 11.5 ความปลอดภัยรหัสผ่าน

- รหัสผ่าน **ไม่ถูกบันทึกลงดิสก์**
- เก็บไว้ในหน่วยความจำระหว่างเซสชันเท่านั้น
- การเชื่อมต่อล่าสุดบันทึกรายละเอียดอื่น แต่ต้องใส่รหัสผ่านใหม่

---

## 12. การแก้ไขปัญหา

### 12.1 ปัญหาการเชื่อมต่อ

**ข้อผิดพลาด "Connection refused":**
- ตรวจสอบว่าเซิร์ฟเวอร์ฐานข้อมูลกำลังรันอยู่
- ตรวจสอบว่าโฮสต์และพอร์ตถูกต้อง
- ตรวจสอบว่าไฟร์วอลล์อนุญาตการเชื่อมต่อ
- สำหรับฐานข้อมูล cloud ตรวจสอบ IP allow-list

**ข้อผิดพลาด "Access denied":**
- ตรวจสอบชื่อผู้ใช้และรหัสผ่าน
- ตรวจสอบว่าผู้ใช้มีสิทธิ์สำหรับฐานข้อมูล
- สำหรับ SQL Server ตรวจสอบว่าเปิดใช้งานการรับรองความถูกต้องของ SQL

**ข้อผิดพลาด SSL:**
- เปิดใช้งานตัวเลือก "ใช้ SSL" สำหรับฐานข้อมูล cloud
- ตรวจสอบใบรับรอง SSL ของเซิร์ฟเวอร์ถูกต้อง
- พยายามปิดใช้งาน SSL เฉพาะสำหรับการพัฒนาในเครื่อง

### 12.2 ปัญหา Query

**ข้อผิดพลาด "Query timeout":**
- เพิ่มหมดเวลาในการตั้งค่า
- เพิ่มเงื่อนไข LIMIT/TOP เพื่อลดขนาดผลลัพธ์
- ปรับปรุง query ด้วย index

**ข้อผิดพลาด "Out of Memory":**
- เปิดใช้งานโหมดสตรีมมิ่งสำหรับผลลัพธ์ขนาดใหญ่
- ลดขีดจำกัดจำนวนแถวสูงสุด
- เพิ่มขนาด heap ของ Java: แก้ไข `run.bat` และเพิ่ม `-Xmx4G`

**ข้อผิดพลาด "Invalid Query":**
- ตรวจสอบไวยากรณ์ SQL สำหรับประเภทฐานข้อมูลของคุณ
- ตรวจสอบว่าชื่อตารางและคอลัมน์มีอยู่
- ตรวจสอบคำสำคัญสงวน

### 12.3 ปัญหา CSV

**ข้อผิดพลาด "Table not found":**
- ตรวจสอบว่ามีไฟล์ CSV ในโฟลเดอร์ที่เลือก
- ใช้เครื่องหมายคำพูดคู่รอบชื่อตาราง: `"customers"`
- ตรวจสอบนามสกุลไฟล์เป็น `.csv`

**"Garbled text" ใน CSV:**
- ตรวจสอบการเข้ารหัสไฟล์ต้นฉบับ
- H2 ตรวจจับ UTF-8, TIS-620, Windows-874 โดยอัตโนมัติ
- พยายามแปลงไฟล์เป็น UTF-8 ก่อนโหลด

### 12.4 ปัญหาแอปพลิเคชัน

**แอปพลิเคชันไม่เริ่ม:**
- ตรวจสอบว่าติดตั้ง Java 17+: `java -version`
- ตรวจสอบว่า `run.bat` มีอยู่และสามารถเรียกใช้ได้
- ตรวจสอบเอาต์พุตคอนโซลสำหรับข้อผิดพลาด

**UI ดูแปลก:**
- ตรวจสอบว่าการปรับขนาดหน้าจอตั้งค่าเป็น 100% หรือ 125%
- อัปเดตไดรเวอร์กราฟิก
- พยายามขยายหน้าต่างให้เต็มจอ

---

## 13. คีย์ลัด

| คีย์ลัด | การกระทำ |
|----------|--------|
| Ctrl+Enter | รัน query ปัจจุบัน |
| Ctrl+F | จัดรูปแบบ SQL |
| Ctrl+C | คัดลอกเซลล์ที่เลือก |
| Escape | ล้างการเลือก |

---

## ภาคผนวก A: ความแตกต่างของ SQL Dialect

| ฟีเจอร์ | MySQL | PostgreSQL | SQL Server | CSV (H2) |
|---------|-------|------------|------------|----------|
| การจำกัดแถว | `LIMIT n` | `LIMIT n` | `TOP n` | `LIMIT n` |
| เครื่องหมายคำพูดตัวระบุ | `` `name` `` | `"name"` | `[name]` | `"name"` |
| การต่อข้อความ | `CONCAT()` | `\|\|` | `+` | `\|\|` |
| วันที่ปัจจุบัน | `CURDATE()` | `CURRENT_DATE` | `GETDATE()` | `CURRENT_DATE()` |
| Like ไม่คำนึงถึงตัวพิมพ์ | `LIKE` | `ILIKE` | `LIKE` | `LIKE` |
| Boolean | `1/0` | `true/false` | `1/0` | `TRUE/FALSE` |

---

## ภาคผนวก B: ฟังก์ชัน SQL ที่รองรับ (โหมด CSV/H2)

**ฟังก์ชันรวม:**
- `COUNT()`, `SUM()`, `AVG()`, `MAX()`, `MIN()`

**ฟังก์ชันข้อความ:**
- `UPPER()`, `LOWER()`, `CONCAT()`, `SUBSTRING()`

**ฟังก์ชันวันที่:**
- `CURRENT_DATE()`, `CURRENT_TIMESTAMP()`

**ฟังก์ชันคณิตศาสตร์:**
- `ROUND()`, `ABS()`, `MOD()`

**ฟังก์ชันเงื่อนไข:**
- `COALESCE()`, `NULLIF()`, `CASE WHEN`

---

**เวอร์ชันเอกสาร**: 1.1.0  
**อัปเดตล่าสุด**: มีนาคม 2026  
**แอปพลิเคชัน**: QueryCraft Database Query Tool
